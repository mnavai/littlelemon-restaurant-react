const NavLink = ({text}) => {
    return (
        <ul className="list-item">
            <li className="">
                <a href="">{text}</a>
            </li>
        </ul>
    );
}
export default NavLink;